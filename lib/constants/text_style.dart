import 'package:flutter/material.dart';
import 'package:zheeta/constants/color.dart';

// String formattting across the app

const headerStyle =
    TextStyle(color: white, fontSize: 20, fontWeight: FontWeight.bold);
const titleStyle = TextStyle(color: white, fontSize: 20);
const subtitleStyle = TextStyle(color: white, fontSize: 13);

const authTitleStyle =
    TextStyle(color: grayscale, fontSize: 32, fontWeight: FontWeight.bold);
const authSubtitleStyle =
    TextStyle(color: primaryDark, fontSize: 14, fontWeight: FontWeight.normal);
const authAltStyle =
    TextStyle(color: grayscale, fontSize: 14, fontWeight: FontWeight.normal);

const forgotTitleStyle =
    TextStyle(color: primaryDark, fontSize: 18, fontWeight: FontWeight.normal);
const forgotSubtitleStyle =
    TextStyle(color: grayscale, fontSize: 14, fontWeight: FontWeight.normal);
